bucket = "airport-mc-terraform-staging"
# State prefix is supplied per stack via Makefile init flags.
credentials = "path/to/service-account.json" # TODO: provide CI/workstation credentials or remove when using ADC.
